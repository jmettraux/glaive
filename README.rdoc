
= glaive

a golang experiment


== [un]features

- key
- each record is a JSON file


== API

All replies are encoded in JSON.
Returns a string in case of error.

put {type}\r\n {json_doc}\r\n
get {type} {key}\r\n
delete {type} {key}\r\n


== license

MIT

